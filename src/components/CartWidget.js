import React from 'react';

const CartWidget = () => {
  return (
    <span
      style={{ justifySelf: 'flex-end', marginRight: '1rem', color: '#61dafb' }}
      className="material-symbols-outlined"
    >
      shopping_cart
    </span>
  );
};

export default CartWidget;
